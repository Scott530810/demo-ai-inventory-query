# Server Requirements for DGX SPARK Deployment
# FastAPI 服務器依賴

# Core dependencies (same as main project)
psycopg2-binary==2.9.9
requests==2.31.0
python-dotenv==1.0.0

# API Server dependencies
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3

# Optional: for production deployment
gunicorn==21.2.0
